# Dockerfile.api - FastAPI Backend
FROM python:3.11-slim
WORKDIR /app
COPY backend ./backend
WORKDIR /app/backend
RUN pip install --upgrade pip && pip install -r pyproject.toml
EXPOSE 8000
CMD ["uvicorn", "lambda_entrypoint:app", "--host", "0.0.0.0", "--port", "8000"]
